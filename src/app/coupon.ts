export class Coupon {
  id: number;
code: string;
type: number;
maxDisc: number;
minAmount: number;
discValue: number;
  expiry: Date;
//  id` int(4) NOT NULL AUTO_INCREMENT,
// `code` tinytext,
// `type` int(2),
// `max_disc` double,
// `min_amount` double,
// `disc_value` double,
// `expiry` date
}